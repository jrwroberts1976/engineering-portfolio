<footer class="site-footer">
  <div class="container footer-grid">
    <div>
      <strong>James Roberts</strong>

      <p>
        Infrastructure, Linux, cloud, platform engineering,
        observability and security.
      </p>
    </div>

    <div class="footer-links">
      <a href="https://github.com/jrwroberts1976">GitHub</a>
      <a href="/projects">Projects</a>
      <a href="/contact">Contact</a>
    </div>
  </div>

  <div class="container footer-bottom">
    <span>© {new Date().getFullYear()} James Roberts</span>
    <span>Built with Astro and deployed using Docker</span>
  </div>
</footer>

<style>
  .site-footer {
    border-top: 1px solid var(--border);
    background: rgba(0, 0, 0, 0.12);
  }

  .footer-grid {
    display: grid;
    grid-template-columns: 1fr auto;
    gap: 3rem;
    padding-block: 3.5rem 2rem;
  }

  .footer-grid p {
    max-width: 500px;
    color: var(--text-muted);
  }

  .footer-links {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    align-content: start;
  }

  .footer-links a {
    color: var(--text-muted);
    text-decoration: none;
  }

  .footer-links a:hover {
    color: var(--primary);
  }

  .footer-bottom {
    display: flex;
    justify-content: space-between;
    gap: 2rem;
    padding-block: 1.25rem;
    border-top: 1px solid var(--border);
    color: var(--text-muted);
    font-size: 0.85rem;
  }

  @media (max-width: 700px) {
    .footer-grid,
    .footer-bottom {
      grid-template-columns: 1fr;
    }

    .footer-bottom {
      flex-direction: column;
      gap: 0.4rem;
    }
  }
</style>
