<!--
|--------------------------------------------------------------------------
| Homepage Architecture Preview
|--------------------------------------------------------------------------
|
| Provides a simplified view of the engineering platform and links to
| the complete architecture page.
|
-->

<section class="section section-soft">
  <div class="container architecture-preview">
    <div class="section-heading">
      <p class="eyebrow">Platform Architecture</p>

      <h2>One connected engineering environment.</h2>

      <p>
        Edge protection, identity, containers, applications, monitoring and
        security controls work together as a complete operational platform.
      </p>

      <a class="button button-primary" href="/architecture">
        Explore the architecture
      </a>
    </div>

    <div class="mini-architecture" aria-label="Simplified architecture">
      <div class="mini-node">
        <span>Edge</span>
        <strong>Cloudflare</strong>
      </div>

      <div class="mini-arrow">→</div>

      <div class="mini-node">
        <span>Access</span>
        <strong>Nginx and Authelia</strong>
      </div>

      <div class="mini-arrow">→</div>

      <div class="mini-node">
        <span>Platform</span>
        <strong>Docker and Kubernetes</strong>
      </div>

      <div class="mini-support">
        <div class="mini-node">
          <span>Observability</span>
          <strong>Prometheus and Grafana</strong>
        </div>

        <div class="mini-node">
          <span>Security</span>
          <strong>CrowdSec and Firewall</strong>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .architecture-preview {
    display: grid;
    grid-template-columns: minmax(0, 0.8fr) minmax(400px, 1.2fr);
    align-items: center;
    gap: 4rem;
  }

  .mini-architecture {
    display: grid;
    gap: 0.75rem;
  }

  .mini-node {
    display: grid;
    gap: 0.25rem;
    padding: 1.1rem 1.25rem;
    border: 1px solid var(--border);
    border-radius: 13px;
    background: var(--surface);
  }

  .mini-node span {
    color: var(--primary);
    font-size: 0.72rem;
    font-weight: 800;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .mini-arrow {
    text-align: center;
    color: var(--primary);
    font-size: 1.4rem;
    transform: rotate(90deg);
  }

  .mini-support {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 0.75rem;
    margin-top: 0.5rem;
  }

  @media (max-width: 900px) {
    .architecture-preview,
    .mini-support {
      grid-template-columns: 1fr;
    }

    .architecture-preview {
      gap: 2rem;
    }
  }
</style>
